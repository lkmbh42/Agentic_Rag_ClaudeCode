Alembic migrations.

The initial migration is autogenerated from app/models. To create new revisions
after changing models (run inside the backend image so deps + app are present):

  alembic revision --autogenerate -m "describe change"
  alembic upgrade head

The DB URL comes from env.py (settings.database_url_sync, or ALEMBIC_DATABASE_URL).
