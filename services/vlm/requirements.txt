# Phase 1 stub. Phase 3 adds the model stack (CPU):
#   torch (cpu), transformers, qwen-vl-utils, accelerate, pillow
fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic==2.*
# openai VLM backend (captions via a local Ollama vision model, OpenAI-compatible).
httpx==0.28.*
