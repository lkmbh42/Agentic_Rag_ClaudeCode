[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    docling: slow layout-model parsing tests (deselect with -m "not docling")
    llm: tests that call the real LLM backend (deselect with -m "not llm")
filterwarnings =
    ignore::DeprecationWarning
